/**
 * @dotenvup/format — Format constants
 */

export const FORMAT_MAGIC = '#!dotenvup';
export const FORMAT_VERSION = 1;
export const DEFAULT_ALGORITHM = 'x25519-xchacha20-poly1305';
export const DEFAULT_RECIPIENT = '@local';
